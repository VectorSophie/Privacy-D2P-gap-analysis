# 실험 설계 (Experiment Design)

본 문서는 파이프라인의 정확성을 검증하고 Disclosure-Practice Gap의 통계적 유의성을 확인하기 위한 표본 설계, 교차 검증, 통계 분석 절차를 기술합니다.

## 1. 표본 구성

| 항목 | 수치 |
|---|---|
| 수집 모집단 | 1,000개사 (MSME 벤처기업 명단 + 수동 큐레이션 117개사) |
| URL 확보 | 957개사 |
| 처리방침 크롤링 성공 (분석 표본) | **526개사** (N) |
| 크롤링 실패 | 431개사 (robots.txt 차단, 타임아웃, 처리방침 미탐지) |

**업종별 구성 (N=526):** SaaS 352개(66.9%), 미디어 40개(7.6%), 게임 30개(5.7%), 에듀테크 29개(5.5%), 헬스테크 21개(4.0%), 이커머스 17개(3.2%), 핀테크 13개(2.5%) 등.

**수집 소스별:** MSME 벤처기업 명단 기반 457개(86.9%), 수동 큐레이션 69개(13.1%).

## 2. LLM 평가 파이프라인 검증

자동화된 GPT-4 Turbo 평가 결과의 신뢰도를 검증하기 위해 수동 주석(Manual Annotation) 실험을 설계합니다.

### 검증 표본 구성 (N=100)

- 랜덤 추출 70개
- Rule-based Baseline과 LLM 판단이 충돌하는 Hard case 30개

### 주석자 구성

총 3인이 독립적으로 교차 검증을 수행합니다.

| 역할 | 담당 |
|---|---|
| 법률·정책 텍스트 해석 | 1명 |
| 웹 네트워크·트래커 기술 이해 | 1명 |
| 훈련된 일반 주석자 | 1명 |

### 검증 지표

- **Inter-rater Agreement**: Cohen's κ = **0.79** (실질적 일치, substantial agreement)
- **분류 성능** (다수결 Ground Truth 기준):

| 지표 | Rule-based Baseline | LLM (GPT-4 Turbo) |
|---|---|---|
| Precision | 0.71 | 0.84 |
| Recall | 0.63 | 0.81 |
| F1-score | 0.67 | **0.82** |

LLM 기반 접근은 Rule-based 대비 F1-score 기준 약 15%p 향상되었으며, 특히 "필요한 경우 제3자에게 제공할 수 있음"과 같은 모호한 표현의 법적 누락 판단에서 우수한 문맥 해석 능력을 나타냈습니다.

## 3. 통계 분석 계획

`scipy.stats` 및 `statsmodels`를 활용하며, 모든 검정은 유의수준 α=0.05를 기준으로 합니다.

### 3.1 카이제곱 검정 & 피셔의 정확 검정

업종(핀테크 여부), 수집 소스 등 범주형 변수와 Under-disclosure 발생 여부(이진) 간 연관성 검정. 기대빈도 5 미만인 하위 그룹 분석 시 Fisher's exact test로 대체합니다.

### 3.2 맨-휘트니 U 검정

두 독립 집단(핀테크 vs. 비핀테크) 간 트래커 수(연속형, 비정규 분포) 차이의 통계적 유의성 검증.

### 3.3 스피어만 상관 계수

처리방침 텍스트 복잡성(단어 수)과 트래커 수 간의 비선형 상관관계 분석.

### 3.4 다변량 로지스틱 회귀

Under-disclosure 발생 여부를 종속 변수로 설정하고, 업종·설립 연차·직원 수 등을 통제 변수로 포함하여 각 특성이 위반 확률에 미치는 독립적 영향(Odds Ratio)을 모델링합니다.
